#include <stdio.h>

/**
 * main -  helps prints a string with the printf function
 *
 * Return:  returns a 0 if no error else -1
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
